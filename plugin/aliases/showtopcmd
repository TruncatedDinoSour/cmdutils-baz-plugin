fc -l 1 | $(which awk) '{CMD[$2]++;count++;}END { for (a in CMD)print CMD[a] " " CMD[a]/count*100 "% " a;}' | $(which grep) -v './' | $(which column) -c3 -s ' ' -t | $(which sort) -nr | $(which nl) | $(which head) -n
